# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.syso

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

release
tmp
webview/webview
webview/webview.exe

# Web UI toolchain output (sources in app/web/ui, build lands in app/web/statics)
app/web/ui/node_modules/
app/web/ui/dist/
app/web/ui/test-results/
app/web/ui/playwright-report/
